name: Staged Content Boundary Canary

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "STAGED_CONTENT_WORKFLOW_CANARY"