name: Path Probe double_encoded

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PATH_DOUBLE_ENCODED_1784825619"
