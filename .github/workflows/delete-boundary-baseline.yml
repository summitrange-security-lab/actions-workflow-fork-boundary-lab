name: Owner Staged Delete Boundary Baseline

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "OWNER_STAGED_DELETE_BASELINE"